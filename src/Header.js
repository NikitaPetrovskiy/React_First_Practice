import React from 'react';

function Header () {
    return (
        <header>
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Contacts</a>
        </header>
    )
};

export default Header;